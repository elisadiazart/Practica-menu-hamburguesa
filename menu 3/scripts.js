const menuElement = document.getElementById('icon-menu')
const lineElement = document.getElementById('line')

menuElement.addEventListener('click', () => {
    line.classList.toggle('line-arrow-right')
});